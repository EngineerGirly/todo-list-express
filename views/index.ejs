<!DOCTYPE html> <!--telling the browser the type of document will be displayed-->
<html lang="en"> <!--start of the html tag and setting the language to english-->
<head> <!--starting the headtag to store our metadata-->
    <meta charset="UTF-8"> <!--indicates what charater set should be used-->
    <meta http-equiv="X-UA-Compatible" content="IE=edge"> <!--sets appropriate compatability for Microsoft browsers-->
    <meta name="viewport" content="width=device-width, initial-scale=1.0"> <!--sets the width of available viewing area to the width of device screen-->
    <title>Document</title> <!--sets a title that appears on the browser tab-->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css"> <!--creates a link to the font awesome icon library-->
    <link rel="stylesheet" href="css/style.css"> <!--linking to our local css file in the public folder-->
</head> <!--closes the head tag-->
<body>
    <!DOCTYPE html>
<html lang="en">
<head> <!--This might be a type-->
    <meta charset="UTF-8"> <!--This might be a type-->
    <meta http-equiv="X-UA-Compatible" content="IE=edge"> <!--This might be a type-->
    <meta name="viewport" content="width=device-width, initial-scale=1.0"> <!--This might be a type-->
    <title>Document</title> <!--This might be a type-->
</head> <!--This might be a type-->
<body> <!--main section for content displayed-->
    <h1>Todo List: </h1> <!--Main title displayed on page-->
    <ul class="todoItems"> <!--creating an unordered list and assign class to the list-->
    <% for(let i=0; i < items.length; i++) {%> <!--use ejs to start a for loop-->
        <li class="item"> <!--create a list item with a class of item-->
            <% if(items[i].completed === true) {%> <!--conditional if statement using ejs that checks if an item in an array with a completed property of true-->
                <span class='completed'><%= items[i].thing %></span> <!--creating an inline element with a class and adding ejs to display the name of the task -->
            <% }else{ %> <!--ejs for the else clause of our if statement-->
                <span><%= items[i].thing %></span> <!--using ejs to display the name of the task with NO CLASS-->
            <% } %>  <!--ejs to close the else statement-->  
            <span class='fa fa-trash'></span> <!--display font awesome icon of trash can inside a span-->
        </li> <!--closing our list item-->
    <% } %> <!--using ejs to close the for loop-->
    </ul> <!-- the closing tag for our unordered list-->

    <h2>Left to do: <%= left %></h2> <!--Creates a second level heading tag with text and using EJS to display a variable showing how many tasks are left to complete-->

    <h2>Add A Todo:</h2> <!--Creates a second level header-->

    <form action="/addTodo" method="POST"> <!--Create a form with a POST method that goes to the /addTodo route-->
        <input type="text" placeholder="Thing To Do" name="todoItem"> <!--creates an input box that accepts text with a default text inside of it before you type anything. Input box is also given a name for later refernce-->
        <input type="submit"> <!--gives the form a submit button-->
    </form> <!--closing the form tag-->
    

    <script src='js/main.js'></script> <!--links to the main.js file in the js folder in the public folder-->
</body> <!--closes the body tag-->
</html> <!--closes the html tag-->
